# Authentication
AUTH_SECRET="your-auth-secret-here"
AUTH_URL="http://localhost:3000"

# Database (Docker)
DATABASE_URL="postgresql://boxframe:boxframe@localhost:5433/boxframe?schema=public"

# OpenLDAP (Docker - Port 390)
# Admin DN: cn=admin,dc=boxframe,dc=local
# Admin Password: admin
# User Base DN: ou=users,dc=boxframe,dc=local
# LDAP Port: 390 (default 389 is used by box1)
