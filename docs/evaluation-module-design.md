# 人事評価モジュール 設計書

## 1. 設計思想

### サンプル(ted-box1)との差別化

| 観点 | サンプル | 新設計 |
|------|----------|--------|
| 複雑さ | 多機能・多テーブル | 整理されたテーブル構成 |
| 評価基準 | 3軸（結果・プロセス・成長） | **3軸を継承** |
| ワークフロー | 5フェーズ | 3フェーズに簡略化 |
| 評価者決定 | カスタム関係テーブル | **カスタム評価者を継承** |
| 重み設定 | 役職別・等級別 | **資格等級別の重み設定を継承** |

### 設計原則

1. **組織モジュールとの統合**: 既存の階層・責任者情報を最大活用
2. **3軸評価の継承**: 結果・プロセス・成長の包括的評価
3. **カスタム評価者**: 組織階層 + カスタム設定の両対応
4. **柔軟な重み設定**: 資格等級別に重みを設定可能
5. **ユーザビリティ**: 直感的なUI/UX

---

## 2. 評価の基本構造

### 2.1 評価軸（3軸）

#### 基準1: 結果評価（Results）
- **評価対象**: 組織の業績達成度
- **評価方法**:
  - 所属組織の目標達成率を基準にスコア算出
  - 達成率がそのままスコアに反映（100% = 3.0、120% = 3.6 など）
- **データソース**: 組織別の業績目標・実績データ

#### 基準2: プロセス評価（Process）
- **評価対象**: 日常業務における行動・取り組み姿勢
- **評価項目**（5つの行動特性）:
  - 主体性: 自ら考え、積極的に行動
  - 協調性: チーム内での協力
  - 責任感: 役割達成への意識
  - 改善力: より良い方法を模索
  - 専門性: 業務に必要な知識・スキル
- **評価方法**: 各項目T1〜T4の4段階 → 平均スコア
- **評価者**: 直属上長

#### 基準3: 成長評価（Growth）
- **評価対象**: 評価期間中における社員の成長・発展
- **成長カテゴリー**（最も顕著な1つを選択）:
  - 専門スキル向上
  - 資格取得・認定
  - 業務知識深化
  - 部下・後輩指導
  - リーダーシップ発揮
  - 問題解決・改善提案
  - 新規領域への挑戦
- **評価方法**: T1〜T4の4段階 × カテゴリー係数
- **評価者**: 直属上長

### 2.2 評価グレード

| グレード | スコア範囲 | 意味 |
|----------|-----------|------|
| S | 4.5 - 5.0 | 卓越 |
| A | 3.5 - 4.4 | 優秀 |
| B | 2.5 - 3.4 | 標準 |
| C | 1.5 - 2.4 | 要改善 |
| D | 1.0 - 1.4 | 不十分 |

### 2.3 重み設定

資格等級別に3軸の重み配分を設定：

| 資格等級 | 結果評価 | プロセス評価 | 成長評価 |
|----------|----------|--------------|----------|
| G1-G2（一般） | 20% | 40% | 40% |
| G3-G4（主任級） | 30% | 40% | 30% |
| G5-G6（管理職） | 40% | 35% | 25% |
| G7以上（上級管理職） | 50% | 30% | 20% |

※ デフォルト値。管理画面から変更可能。

---

## 3. ワークフロー（3フェーズ）

```
┌─────────────────────────────────────────────────────────────┐
│ Phase 1: 準備（ADMIN）                                      │
│ ├─ 評価期間を作成（年度・期）                               │
│ ├─ 重み配分を設定（業績:行動 = 60:40 など）                  │
│ └─ 評価データを一括生成                                     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Phase 2: 評価入力（MANAGER）                                │
│ ├─ 部下一覧を確認                                          │
│ ├─ 業績評価スコアを入力                                     │
│ ├─ 行動評価（4項目）を入力                                  │
│ ├─ 総合コメントを記入                                       │
│ └─ 評価を確定                                               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Phase 3: 確認・フィードバック（USER/MANAGER）               │
│ ├─ 社員が自身の評価結果を確認                               │
│ ├─ 1on1面談の実施（オプション）                             │
│ └─ 評価期間のクローズ                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. データモデル

### 4.1 ERD概要

```
┌─────────────────┐      ┌─────────────────┐
│EvaluationPeriod │      │EvaluationWeight │ 重み設定
├─────────────────┤      ├─────────────────┤
│ id              │◄─────│ periodId        │
│ name            │      │ gradeCode       │ (G1,G2...)
│ year            │      │ resultsWeight   │
│ term            │      │ processWeight   │
│ status          │      │ growthWeight    │
│ startDate       │      └─────────────────┘
│ endDate         │
└────────┬────────┘
         │
         │  ┌─────────────────┐
         │  │OrganizationGoal │ 組織目標・実績
         │  ├─────────────────┤
         │  │ periodId        │
         │  │ departmentId    │
         │  │ targetProfit    │
         │  │ actualProfit    │
         │  │ achievementRate │
         │  └─────────────────┘
         │
┌────────┴────────┐      ┌─────────────────┐
│   Evaluation    │      │CustomEvaluator  │ カスタム評価者
├─────────────────┤      ├─────────────────┤
│ id              │      │ employeeId      │
│ periodId        │      │ evaluatorId     │
│ employeeId      │◄─────│ periodId        │
│ evaluatorId     │      │ effectiveFrom   │
│ score1 (結果)   │      │ effectiveTo     │
│ score2 (プロセス)│      └─────────────────┘
│ score3 (成長)   │
│ weightedScore1  │      ┌─────────────────┐
│ weightedScore2  │      │ProcessCategory  │ プロセス評価項目
│ weightedScore3  │      ├─────────────────┤
│ finalScore      │      │ id              │
│ finalGrade      │      │ name            │
│ status          │      │ description     │
└─────────────────┘      │ sortOrder       │
                         └─────────────────┘
┌─────────────────┐
│    Employee     │      ┌─────────────────┐
├─────────────────┤      │GrowthCategory   │ 成長評価カテゴリ
│ department      │      ├─────────────────┤
│ section         │      │ id              │
│ course          │      │ name            │
│ position        │      │ coefficient     │ 係数
│ qualificationGradeCode│ │ sortOrder       │
└─────────────────┘      └─────────────────┘
```

### 4.2 Prismaスキーマ

```prisma
// =====================================================
// 評価期間
// =====================================================
model EvaluationPeriod {
  id                String   @id @default(cuid())
  name              String   // "2024年度 上期評価"
  year              Int      // 2024
  term              String   // "H1" | "H2" | "ANNUAL"
  status            EvaluationPeriodStatus @default(DRAFT)

  // 期間
  startDate         DateTime
  endDate           DateTime

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  evaluations       Evaluation[]
  goals             EvaluationGoal[]
  weights           EvaluationWeight[]
  organizationGoals OrganizationGoal[]
  customEvaluators  CustomEvaluator[]
}

enum EvaluationPeriodStatus {
  DRAFT       // 準備中
  ACTIVE      // 評価実施中
  REVIEW      // レビュー期間
  CLOSED      // 完了
}

// =====================================================
// 重み設定（資格等級別）
// =====================================================
model EvaluationWeight {
  id                String   @id @default(cuid())

  periodId          String
  period            EvaluationPeriod @relation(fields: [periodId], references: [id], onDelete: Cascade)

  gradeCode         String   // "G1", "G2", ... or "DEFAULT"

  // 重み（%、合計100）
  resultsWeight     Int      @default(30)   // 結果評価
  processWeight     Int      @default(40)   // プロセス評価
  growthWeight      Int      @default(30)   // 成長評価

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique([periodId, gradeCode])
}

// =====================================================
// 組織目標・実績（結果評価用）
// =====================================================
model OrganizationGoal {
  id                String   @id @default(cuid())

  periodId          String
  period            EvaluationPeriod @relation(fields: [periodId], references: [id], onDelete: Cascade)

  // 対象組織（Department/Section/Course のいずれか）
  organizationType  String   // "DEPARTMENT" | "SECTION" | "COURSE"
  organizationId    String
  organizationName  String

  // 目標・実績
  targetProfit      Float?   // 目標利益
  actualProfit      Float?   // 実績利益
  achievementRate   Float?   // 達成率（%）

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique([periodId, organizationType, organizationId])
  @@index([periodId])
}

// =====================================================
// カスタム評価者関係
// =====================================================
model CustomEvaluator {
  id                String   @id @default(cuid())

  employeeId        String
  employee          Employee @relation("customEvaluatee", fields: [employeeId], references: [id], onDelete: Cascade)

  evaluatorId       String
  evaluator         Employee @relation("customEvaluator", fields: [evaluatorId], references: [id], onDelete: Cascade)

  periodId          String?  // null = 全期間有効
  period            EvaluationPeriod? @relation(fields: [periodId], references: [id], onDelete: Cascade)

  // 有効期間（期間を限定する場合）
  effectiveFrom     DateTime?
  effectiveTo       DateTime?

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique([employeeId, periodId])
  @@index([evaluatorId])
}

// =====================================================
// プロセス評価項目マスタ
// =====================================================
model ProcessCategory {
  id                String   @id @default(cuid())
  name              String   // "主体性"
  nameEn            String?  // "Initiative"
  description       String?  // 説明文
  sortOrder         Int      @default(0)
  isActive          Boolean  @default(true)

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}

// =====================================================
// 成長評価カテゴリマスタ
// =====================================================
model GrowthCategory {
  id                String   @id @default(cuid())
  name              String   // "専門スキル向上"
  nameEn            String?  // "Professional Skill Improvement"
  description       String?  // 説明文
  coefficient       Float    @default(1.0)  // スコア係数
  sortOrder         Int      @default(0)
  isActive          Boolean  @default(true)

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}

// =====================================================
// 評価データ
// =====================================================
model Evaluation {
  id                String   @id @default(cuid())

  // 関連
  periodId          String
  period            EvaluationPeriod @relation(fields: [periodId], references: [id], onDelete: Cascade)
  employeeId        String
  employee          Employee @relation("evaluations", fields: [employeeId], references: [id])
  evaluatorId       String?
  evaluator         Employee? @relation("evaluationsGiven", fields: [evaluatorId], references: [id])

  // スナップショット（評価時点の情報）
  departmentName    String?
  sectionName       String?
  courseName        String?
  positionName      String?
  gradeCode         String?  // 資格等級コード

  // ========== 基準1: 結果評価 ==========
  score1            Float?   // 結果評価スコア（自動計算）
  score1Comment     String?

  // ========== 基準2: プロセス評価 ==========
  // JSON: {"主体性": 3, "協調性": 4, ...}
  processScores     Json?
  score2            Float?   // プロセス評価スコア（平均）
  score2Comment     String?

  // ========== 基準3: 成長評価 ==========
  growthCategoryId  String?  // 選択した成長カテゴリ
  growthLevel       Int?     // T1=1, T2=2, T3=3, T4=4
  score3            Float?   // 成長評価スコア（レベル × 係数）
  score3Comment     String?

  // ========== 最終評価 ==========
  // 加重スコア（保存用）
  weightedScore1    Float?
  weightedScore2    Float?
  weightedScore3    Float?

  finalScore        Float?   // 最終スコア
  finalGrade        String?  // S/A/B/C/D
  overallComment    String?  // 総合コメント

  // ステータス
  status            EvaluationStatus @default(PENDING)

  // メタ情報
  evaluatedAt       DateTime?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique([periodId, employeeId])
  @@index([periodId])
  @@index([employeeId])
  @@index([evaluatorId])
}

enum EvaluationStatus {
  PENDING     // 未評価
  IN_PROGRESS // 評価中
  COMPLETED   // 評価完了
  CONFIRMED   // 確定済み
}

// =====================================================
// 目標設定（オプション機能）
// =====================================================
model EvaluationGoal {
  id                String   @id @default(cuid())

  periodId          String
  period            EvaluationPeriod @relation(fields: [periodId], references: [id], onDelete: Cascade)
  employeeId        String
  employee          Employee @relation(fields: [employeeId], references: [id])

  // 目標（JSON配列）
  goals             Json     @default("[]")
  // 自己振り返り
  selfReflection    String?

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique([periodId, employeeId])
}
```

---

## 5. 評価者決定ロジック

### 決定の優先順位

```typescript
async function determineEvaluator(
  employee: Employee,
  periodId: string
): Promise<Employee | null> {
  // ========================================
  // 優先順位1: カスタム評価者（最優先）
  // ========================================
  const customEvaluator = await prisma.customEvaluator.findFirst({
    where: {
      employeeId: employee.id,
      OR: [
        { periodId: periodId },  // 期間指定
        { periodId: null },       // 全期間有効
      ],
      // 有効期間チェック
      OR: [
        { effectiveFrom: null },
        { effectiveFrom: { lte: new Date() } },
      ],
      OR: [
        { effectiveTo: null },
        { effectiveTo: { gte: new Date() } },
      ],
    },
    include: { evaluator: true },
    orderBy: { periodId: 'desc' },  // 期間指定優先
  });

  if (customEvaluator) {
    return customEvaluator.evaluator;
  }

  // ========================================
  // 優先順位2: 課の責任者（課長）
  // ========================================
  if (employee.course?.managerId &&
      employee.course.managerId !== employee.id) {
    return employee.course.manager;
  }

  // ========================================
  // 優先順位3: 部の責任者（部長）
  // ========================================
  if (employee.section?.managerId &&
      employee.section.managerId !== employee.id) {
    return employee.section.manager;
  }

  // ========================================
  // 優先順位4: 本部の責任者（本部長）
  // ========================================
  if (employee.department?.managerId &&
      employee.department.managerId !== employee.id) {
    return employee.department.manager;
  }

  // 評価者なし（手動設定が必要）
  return null;
}
```

### カスタム評価者の設定

**設定場所**: 管理画面の評価マスタ → 評価関係タブ

**設定パターン**:
- **全期間有効**: `periodId = null` で常に適用
- **期間限定**: 特定の評価期間のみ有効
- **日付限定**: `effectiveFrom` / `effectiveTo` で有効期間を指定

**ユースケース**:
- プロジェクト単位での評価者変更
- 兼務者の評価
- 管理職不在時の代理評価

---

## 6. スコア計算

### 6.1 重み取得ロジック

```typescript
async function getWeights(
  periodId: string,
  gradeCode: string | null
): Promise<{ results: number; process: number; growth: number }> {
  // 1. 資格等級別の重みを検索
  if (gradeCode) {
    const gradeWeight = await prisma.evaluationWeight.findUnique({
      where: { periodId_gradeCode: { periodId, gradeCode } },
    });
    if (gradeWeight) {
      return {
        results: gradeWeight.resultsWeight,
        process: gradeWeight.processWeight,
        growth: gradeWeight.growthWeight,
      };
    }
  }

  // 2. デフォルトの重みを検索
  const defaultWeight = await prisma.evaluationWeight.findUnique({
    where: { periodId_gradeCode: { periodId, gradeCode: 'DEFAULT' } },
  });
  if (defaultWeight) {
    return {
      results: defaultWeight.resultsWeight,
      process: defaultWeight.processWeight,
      growth: defaultWeight.growthWeight,
    };
  }

  // 3. ハードコーデッドのデフォルト
  return { results: 30, process: 40, growth: 30 };
}
```

### 6.2 各スコアの算出

#### 基準1: 結果評価（Score1）
```typescript
// 所属組織の達成率からスコアを算出
function calculateResultsScore(achievementRate: number): number {
  // 達成率 100% = 3.0 を基準
  // 80% = 2.4, 120% = 3.6, 150% = 4.5 (上限5.0)
  const score = (achievementRate / 100) * 3.0;
  return Math.min(Math.max(score, 1.0), 5.0);
}
```

#### 基準2: プロセス評価（Score2）
```typescript
// プロセス評価の平均スコア
function calculateProcessScore(
  processScores: Record<string, number>  // {"主体性": 3, "協調性": 4, ...}
): number {
  const values = Object.values(processScores);
  if (values.length === 0) return 0;

  // T1=1, T2=2, T3=3, T4=4 → スコア変換
  // T1=1.0, T2=2.5, T3=3.5, T4=5.0
  const scoreMap = { 1: 1.0, 2: 2.5, 3: 3.5, 4: 5.0 };
  const scores = values.map(v => scoreMap[v] || 2.5);
  return scores.reduce((a, b) => a + b, 0) / scores.length;
}
```

#### 基準3: 成長評価（Score3）
```typescript
// 成長評価スコア = レベル × カテゴリ係数
async function calculateGrowthScore(
  growthCategoryId: string,
  growthLevel: number  // 1-4
): Promise<number> {
  const category = await prisma.growthCategory.findUnique({
    where: { id: growthCategoryId },
  });

  // T1=1.0, T2=2.5, T3=3.5, T4=5.0
  const levelScore = { 1: 1.0, 2: 2.5, 3: 3.5, 4: 5.0 }[growthLevel] || 2.5;
  const coefficient = category?.coefficient || 1.0;

  return Math.min(levelScore * coefficient, 5.0);
}
```

### 6.3 最終スコアの算出

```typescript
interface ScoreResult {
  score1: number;
  score2: number;
  score3: number;
  weightedScore1: number;
  weightedScore2: number;
  weightedScore3: number;
  finalScore: number;
  finalGrade: string;
}

function calculateFinalScore(
  score1: number,      // 結果評価 (1-5)
  score2: number,      // プロセス評価 (1-5)
  score3: number,      // 成長評価 (1-5)
  weights: { results: number; process: number; growth: number }
): ScoreResult {
  // 加重スコア
  const weightedScore1 = score1 * weights.results / 100;
  const weightedScore2 = score2 * weights.process / 100;
  const weightedScore3 = score3 * weights.growth / 100;

  // 最終スコア
  const finalScore = weightedScore1 + weightedScore2 + weightedScore3;

  // グレード判定
  const finalGrade =
    finalScore >= 4.5 ? 'S' :
    finalScore >= 3.5 ? 'A' :
    finalScore >= 2.5 ? 'B' :
    finalScore >= 1.5 ? 'C' : 'D';

  return {
    score1,
    score2,
    score3,
    weightedScore1,
    weightedScore2,
    weightedScore3,
    finalScore,
    finalGrade,
  };
}
```

### 6.4 計算例

| 項目 | 素点 | 重み | 加重スコア |
|------|------|------|-----------|
| 結果評価 | 3.6 | 30% | 1.08 |
| プロセス評価 | 3.5 | 40% | 1.40 |
| 成長評価 | 4.2 | 30% | 1.26 |
| **最終スコア** | - | - | **3.74 (A)** |

---

## 7. API設計

### 7.1 評価期間管理（ADMIN）

| メソッド | エンドポイント | 説明 |
|----------|---------------|------|
| GET | `/api/evaluation/periods` | 評価期間一覧 |
| POST | `/api/evaluation/periods` | 評価期間作成 |
| GET | `/api/evaluation/periods/[id]` | 期間詳細 |
| PATCH | `/api/evaluation/periods/[id]` | 期間更新 |
| DELETE | `/api/evaluation/periods/[id]` | 期間削除 |
| POST | `/api/evaluation/periods/[id]/generate` | 評価データ一括生成 |
| PATCH | `/api/evaluation/periods/[id]/status` | ステータス変更 |

### 7.2 重み設定（ADMIN）

| メソッド | エンドポイント | 説明 |
|----------|---------------|------|
| GET | `/api/evaluation/weights?periodId=xxx` | 重み設定一覧 |
| POST | `/api/evaluation/weights` | 重み設定作成・更新 |
| DELETE | `/api/evaluation/weights/[id]` | 重み設定削除 |

### 7.3 組織目標（ADMIN）

| メソッド | エンドポイント | 説明 |
|----------|---------------|------|
| GET | `/api/evaluation/organization-goals?periodId=xxx` | 組織目標一覧 |
| POST | `/api/evaluation/organization-goals` | 組織目標作成・更新 |
| POST | `/api/evaluation/organization-goals/sync` | 結果評価スコア反映 |

### 7.4 カスタム評価者（ADMIN/MANAGER）

| メソッド | エンドポイント | 説明 |
|----------|---------------|------|
| GET | `/api/evaluation/custom-evaluators` | カスタム評価者一覧 |
| POST | `/api/evaluation/custom-evaluators` | カスタム評価者設定 |
| DELETE | `/api/evaluation/custom-evaluators/[id]` | カスタム評価者削除 |

### 7.5 マスタデータ（ADMIN）

| メソッド | エンドポイント | 説明 |
|----------|---------------|------|
| GET | `/api/evaluation/process-categories` | プロセス評価項目一覧 |
| POST | `/api/evaluation/process-categories` | プロセス評価項目作成 |
| PATCH | `/api/evaluation/process-categories/[id]` | プロセス評価項目更新 |
| GET | `/api/evaluation/growth-categories` | 成長カテゴリ一覧 |
| POST | `/api/evaluation/growth-categories` | 成長カテゴリ作成 |
| PATCH | `/api/evaluation/growth-categories/[id]` | 成長カテゴリ更新 |

### 7.6 評価入力（MANAGER）

| メソッド | エンドポイント | 説明 |
|----------|---------------|------|
| GET | `/api/evaluation/evaluatees` | 被評価者一覧 |
| GET | `/api/evaluation/[id]` | 評価詳細 |
| PATCH | `/api/evaluation/[id]` | 評価入力・更新 |
| POST | `/api/evaluation/[id]/complete` | 評価確定 |

### 7.7 自己確認（USER）

| メソッド | エンドポイント | 説明 |
|----------|---------------|------|
| GET | `/api/evaluation/my-evaluation` | 自分の評価一覧 |
| GET | `/api/evaluation/my-evaluation/[periodId]` | 期間別の自分の評価 |

### 7.8 目標設定（USER）

| メソッド | エンドポイント | 説明 |
|----------|---------------|------|
| GET | `/api/evaluation/goals` | 自分の目標一覧 |
| POST | `/api/evaluation/goals` | 目標設定 |
| PATCH | `/api/evaluation/goals/[id]` | 目標更新 |

---

## 8. UI設計

### 8.1 メニュー構成

| ロール | メニュー | パス | 説明 |
|--------|----------|------|------|
| USER | 自分の評価 | `/my-evaluation` | 評価結果確認・目標設定 |
| MANAGER | 部下評価 | `/manager/evaluations` | 部下の評価入力 |
| ADMIN | 評価管理 | `/admin/evaluation-master` | 期間・マスタ管理 |

### 8.2 管理画面（ADMIN）タブ構成

| タブ | 説明 |
|------|------|
| 評価期間 | 期間の作成・ステータス管理・評価データ生成 |
| 重み設定 | 資格等級別の重み配分設定 |
| 組織目標 | 部門別の業績目標・実績入力 |
| 評価関係 | カスタム評価者の設定 |
| プロセス評価項目 | 行動特性の項目管理 |
| 成長カテゴリ | 成長評価のカテゴリ・係数管理 |

### 8.3 画面構成

#### 自分の評価画面（USER）
```
┌─────────────────────────────────────────────────────────────┐
│ 自分の評価結果                                              │
├─────────────────────────────────────────────────────────────┤
│ [期間選択: 2024年度 上期 ▼]                                 │
│                                                             │
│ ┌───────────────┬───────────────┬───────────────┬─────────┐ │
│ │ 結果評価      │ プロセス評価  │ 成長評価      │ 最終    │ │
│ │   3.6 (30%)   │   3.5 (40%)   │   4.2 (30%)   │ A(3.74) │ │
│ └───────────────┴───────────────┴───────────────┴─────────┘ │
│                                                             │
│ 【結果評価】達成率: 120%                                    │
│  所属組織: データビジネスセンター                           │
│  コメント: 部門目標を大きく上回る成果を達成                 │
│                                                             │
│ 【プロセス評価詳細】                                        │
│  主体性:  ■■■□ (T3)                                        │
│  協調性:  ■■■■ (T4)                                        │
│  責任感:  ■■■□ (T3)                                        │
│  改善力:  ■■□□ (T2)                                        │
│  専門性:  ■■■□ (T3)                                        │
│                                                             │
│ 【成長評価】                                                │
│  カテゴリ: リーダーシップ発揮 (係数: 1.3)                   │
│  達成度: T4                                                 │
│                                                             │
│ 【総合コメント】                                            │
│  今期はチームを牽引し、大きな成果を上げました。             │
│                                                             │
│ ─────────────────────────────────────────────────────────── │
│ 【目標設定・自己振り返り】                                  │
│  [目標設定] [自己振り返り]                                  │
└─────────────────────────────────────────────────────────────┘
```

#### 部下評価画面（MANAGER）
```
┌─────────────────────────────────────────────────────────────┐
│ 部下評価                                      [期間: 2024上期]│
├─────────────────────────────────────────────────────────────┤
│ 評価進捗: 3/5 完了                                          │
│                                                             │
│ ┌──────────────────────────────────────────────────────────┐│
│ │ 氏名       │ 所属    │ 結果 │ プロセス│ 成長 │ 最終│状態││
│ ├──────────────────────────────────────────────────────────┤│
│ │ 田中太郎   │ 営業1課 │ 3.6  │ 3.5     │ 4.2  │ A   │ ✅ ││
│ │ 山田花子   │ 営業1課 │ 3.0  │ 4.0     │ 3.5  │ B   │ ✅ ││
│ │ 佐藤次郎   │ 営業1課 │ 3.6  │ -       │ -    │ -   │ 🔵 ││
│ │ 鈴木三郎   │ 営業2課 │ 3.3  │ 3.2     │ 3.0  │ B   │ ✅ ││
│ │ 高橋四郎   │ 営業2課 │ 3.3  │ -       │ -    │ -   │ 🔵 ││
│ └──────────────────────────────────────────────────────────┘│
│                                                             │
│ ※ 結果評価は組織目標から自動計算                            │
│ ※ 行をクリックして評価入力                                  │
└─────────────────────────────────────────────────────────────┘
```

#### 評価入力ダイアログ
```
┌─────────────────────────────────────────────────────────────┐
│ 評価入力: 佐藤次郎                                    [×]   │
├─────────────────────────────────────────────────────────────┤
│ 所属: PFO本部 > データビジネスセンター > ファシリティG      │
│ 役職: 主任  資格等級: G3  重み: 結果30% プロセス40% 成長30% │
│                                                             │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 【基準1: 結果評価】スコア: 3.6 (自動計算)                   │
│  所属組織達成率: 120%                                       │
│  コメント:                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 部門目標を大きく上回る成果                           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 【基準2: プロセス評価】スコア: 3.50                         │
│  主体性:  [T1] [T2] [●T3] [T4]                              │
│  協調性:  [T1] [T2] [T3] [●T4]                              │
│  責任感:  [T1] [T2] [●T3] [T4]                              │
│  改善力:  [T1] [●T2] [T3] [T4]                              │
│  専門性:  [T1] [T2] [●T3] [T4]                              │
│  コメント:                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ チームへの貢献度が高い。改善提案に期待。             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 【基準3: 成長評価】スコア: 4.55                             │
│  成長カテゴリ: [リーダーシップ発揮 ▼] (係数: 1.3)          │
│  達成度:  [T1] [T2] [T3] [●T4]                              │
│  コメント:                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 後輩指導に積極的に取り組み、チームを牽引             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 【最終評価】スコア: 3.79  グレード: A                       │
│  (結果1.08 + プロセス1.40 + 成長1.37 = 3.85)                │
│                                                             │
│  総合コメント:                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 今期は組織目標の達成に大きく貢献し、チームリーダー   │   │
│  │ として成長が見られた。来期も継続的な成長を期待。     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                        [下書き保存]  [評価確定]             │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. ディレクトリ構造

```
lib/
├── addon-modules/
│   └── evaluation/
│       ├── module.tsx             # モジュール定義
│       ├── evaluator-logic.ts     # 評価者決定ロジック
│       ├── score-calculator.ts    # スコア計算
│       ├── weight-helper.ts       # 重み取得ユーティリティ
│       └── batch-generator.ts     # 一括生成

app/
├── (menus)/
│   ├── (user)/
│   │   └── my-evaluation/
│   │       ├── page.tsx
│   │       ├── MyEvaluationClient.tsx
│   │       ├── translations.ts
│   │       └── components/
│   │           ├── EvaluationSummary.tsx     # 3軸サマリー表示
│   │           ├── ProcessScoreDetail.tsx    # プロセス評価詳細
│   │           ├── GrowthScoreDetail.tsx     # 成長評価詳細
│   │           └── GoalSettingSection.tsx    # 目標設定
│   ├── (manager)/
│   │   └── evaluations/
│   │       ├── page.tsx
│   │       ├── EvaluationsClient.tsx
│   │       ├── translations.ts
│   │       └── components/
│   │           ├── EvaluateeList.tsx         # 被評価者一覧
│   │           ├── EvaluationDialog.tsx      # 評価入力ダイアログ
│   │           ├── ProcessScoreInput.tsx     # プロセス評価入力
│   │           ├── GrowthScoreInput.tsx      # 成長評価入力
│   │           └── ScoreSummary.tsx          # スコアサマリー
│   └── (admin)/
│       └── evaluation-master/
│           ├── page.tsx
│           ├── EvaluationMasterClient.tsx
│           ├── translations.ts
│           └── components/
│               ├── PeriodsSection.tsx        # 評価期間管理
│               ├── WeightsSection.tsx        # 重み設定
│               ├── OrganizationGoalsSection.tsx  # 組織目標
│               ├── CustomEvaluatorsSection.tsx   # カスタム評価者
│               ├── ProcessCategoriesSection.tsx  # プロセス評価項目
│               └── GrowthCategoriesSection.tsx   # 成長カテゴリ
└── api/
    └── evaluation/
        ├── periods/
        │   ├── route.ts                      # 期間一覧・作成
        │   └── [id]/
        │       ├── route.ts                  # 期間詳細・更新・削除
        │       ├── generate/route.ts         # 評価データ一括生成
        │       └── status/route.ts           # ステータス変更
        ├── weights/
        │   ├── route.ts                      # 重み一覧・作成
        │   └── [id]/route.ts                 # 重み更新・削除
        ├── organization-goals/
        │   ├── route.ts                      # 組織目標一覧・作成
        │   └── sync/route.ts                 # スコア反映
        ├── custom-evaluators/
        │   ├── route.ts                      # カスタム評価者一覧・作成
        │   └── [id]/route.ts                 # カスタム評価者削除
        ├── process-categories/
        │   ├── route.ts                      # プロセス評価項目一覧・作成
        │   └── [id]/route.ts                 # 項目更新
        ├── growth-categories/
        │   ├── route.ts                      # 成長カテゴリ一覧・作成
        │   └── [id]/route.ts                 # カテゴリ更新
        ├── evaluatees/route.ts               # 被評価者一覧
        ├── [id]/
        │   ├── route.ts                      # 評価詳細・更新
        │   └── complete/route.ts             # 評価確定
        ├── my-evaluation/
        │   ├── route.ts                      # 自分の評価一覧
        │   └── [periodId]/route.ts           # 期間別の自分の評価
        └── goals/
            ├── route.ts                      # 目標一覧・作成
            └── [id]/route.ts                 # 目標更新
```

---

## 10. 実装フェーズ

### Phase 1: 基盤構築
1. Prismaスキーマ追加
2. モジュール定義作成
3. 基本API実装（CRUD）

### Phase 2: コア機能
4. 評価者決定ロジック
5. 評価データ一括生成
6. スコア計算ロジック

### Phase 3: UI実装
7. 管理画面（評価期間管理）
8. マネージャー画面（評価入力）
9. ユーザー画面（結果確認）

### Phase 4: 仕上げ
10. 翻訳対応
11. テスト
12. ドキュメント

---

## 11. 将来の拡張候補

以下の機能は初期リリースには含めず、要望に応じて追加：

- **360度評価**: 同僚からのフィードバック
- **目標管理(MBO)連携**: 目標設定と評価の紐づけ
- **1on1面談記録**: 面談のスケジュール・記録
- **評価履歴分析**: 過去評価との比較・トレンド
- **評価調整会議**: 部門間の評価すり合わせ
- **通知連携**: 評価期限のリマインダー
